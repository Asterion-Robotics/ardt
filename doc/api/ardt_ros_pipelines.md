# ardt-ros-pipelines

The ROS 2 pipeline plane: the `ros-ci` pipeline and the `ros2` image recipe it renders.

## `ardt_ros_pipelines.ros_ci`

```{eval-rst}
.. automodule:: ardt_ros_pipelines.ros_ci
   :members:
   :undoc-members:
   :show-inheritance:
```
