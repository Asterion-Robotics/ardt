# ardt-ros-dev

The `ros2` dev profile: what a ROS 2 workspace needs in the container {py:mod}`ardt_devcontainers` renders. Data only — one {py:class}`~ardt_devcontainers.profiles.Profile` and one Dockerfile template, registered under `ardt.dev_profiles`.

## `ardt_ros_dev.profile`

```{eval-rst}
.. automodule:: ardt_ros_dev.profile
   :members:
   :undoc-members:
   :show-inheritance:
```
